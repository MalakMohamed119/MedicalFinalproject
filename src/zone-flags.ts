/**
 * This file includes zone.js import for the server side rendering.
 * Zone JS is required by default for Angular itself.
 */

// Disable patch requestAnimationFrame
(window as any).__Zone_disable_requestAnimationFrame = true;

// Disable patch onProperty such as onclick
(window as any).__Zone_disable_on_property = true;

// Disable patch specified eventNames
(window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove', 'resize'];

// Disable patch specified eventNames in the zone.js
// (window as any).__zone_symbol__PASSIVE_EVENTS = ['scroll', 'mousemove', 'resize'];

// Disable patch onProperty such as onclick
(window as any).__Zone_disable_on_property = true;

// Disable patch specified eventNames
(window as any).__zone_symbol__UNPATCHED_EVENTS = ['click', 'mousedown', 'mouseup', 'keydown', 'keyup', 'input'];

// Disable patch onProperty such as onclick
(window as any).__Zone_disable_on_property = true;
